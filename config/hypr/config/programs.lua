-- Программы, которые дёргаются из горячих клавиш (см. keybinds.lua).
-- Поменял тут — поменялось везде, где используется.
return {
    terminal    = "ghostty",
    fileManager = "ghostty -e yazi",
    browser     = "zen-browser",
    menu        = "hyprlauncher",
    uieditor    = "zeditor",
}
